services:
  - type: web
    name: fair-split-api
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn fair_split.app:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
